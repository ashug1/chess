//import axios from 'axios';
import { API } from '../constants/api';
export function convertNumberDetails(inputDetails: any) {
   /* return axios.post(API.ConvertNumber, inputDetails).then((response:any) => {
        return response.data;
    });*/
    return '';
}